# ECC Runtime Store

This directory is created by `python ecc.py init` and used as the local coordination bus for Claude, Codex, and MCP clients.

Runtime files such as `tasks.jsonl`, `events.jsonl`, and `meetings.jsonl` are ignored by git by default.
